import LinkBar from "./LinkBar";

const Logout = () => {
    return <div>
        <LinkBar />
        <div>Are you sure?</div>
        <button onClick={async () => {
            
        }}>yes</button>
    </div>;
};

export default Logout;
